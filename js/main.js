// Leaflet
const buildMap = selection => {

}

// Chart.js
const buildBar = selection => {

}

// Chart.js
const buildLine = selection => {

}

// Event handler on <select> element
const handleChange = selection => {
    buildMap(selection);
    buildBar(selection);
    buildLine(selection);
}
